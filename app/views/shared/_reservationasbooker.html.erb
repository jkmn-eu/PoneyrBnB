  <div class="reservation-card">

    <div>
      <%= image_tag "#{Poney.find(reservationasbooker.poney_id).profile_pic_url}", alt: "Un poney", class: "reservation-card-img"%>
    </div>

    <div class="reservation-card-title">
      <p>Your reservation with </p>
      <p>✨🌟 <%= Poney.find(reservationasbooker.poney_id).name %> 🌟✨</p>
      <p>This reservation is <%= "#{reservationasbooker.status}" %></p>
    </div>

    <div class="reservation-card-txt">
      <ul>
        <li>The owner's email : <%= User.find(Poney.find(reservationasbooker.poney_id).user_id).email %></li>
        <li>Start Date: tbd</li>
        <li>End Date: tbd</li>
      </ul>
    </div>

  </div>
